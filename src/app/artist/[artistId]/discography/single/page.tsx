"use client";
import { useEffect } from "react";

const page = ({ params }: { params: { artistId: string } }) => {
  useEffect(() => {}, []);
  return <div>All albums names</div>;
};
export default page;
